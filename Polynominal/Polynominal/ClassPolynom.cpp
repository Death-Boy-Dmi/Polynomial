#include "ClassPolynom.h"

CPolynom::CPolynom(char * ch, static unsigned int pow)
{

}

CPolynom::CPolynom(string str, static unsigned int pow)
{
	string s = str;
	for (int i = 0; i < s.length; i++)
	{
		if (s[i] == 'x')
		{

		}
	}
}